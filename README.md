[Русский](README.md) | [English](README.en.md)  
![License: Apache-2.0](https://img.shields.io/badge/License-Apache_2.0-blue.svg)

# 202312-todo — Python ToDo / To-Do / TODO app (task manager / task tracker) — CLI + REST API + SQLite

**202312-todo** — open-source **todo app / todo application** для управления задачами (ToDo list / todolist / список задач) на **Python**: консольный **CLI** для ежедневной работы и **HTTP REST API** (экспериментально) для интеграции с веб-интерфейсом, ботами и другими системами.  
Проект покрывает базовый сценарий **CRUD** (Create, Read, Update, Delete): добавление, просмотр, обновление и удаление задач с сохранением данных в **SQLite** базе данных.

## Возможности

- ✅ **Task / ToDo management**: создать задачу, посмотреть список, отметить выполненной, удалить.
- ✅ **CLI (Command Line Interface)**: управление задачами через командную строку.
- 🧪 **REST API (experimental / in progress)**: доступ к задачам по HTTP для интеграций.
- ✅ **SQLite storage**: хранение задач в локальной базе данных.
- ✅ **Logs**: логирование работы CLI в файл.

## Tech stack / Технологии

- Python
- SQLite
- CLI
- REST API (HTTP)
- uv (управление зависимостями и окружением через `pyproject.toml` + `uv.lock`)

## Topics / ключевые слова для поиска (GitHub + SEO)

`python`, `todo`, `to-do`, `todo-app`, `todolist`, `task-manager`, `task-tracker`, `cli`, `command-line`, `rest-api`, `http-api`, `sqlite`, `crud`, `open-source`

## Поддерживаемые окружения

Приложение разрабатывалось и тестировалось в следующих окружениях:

- Windows 10 / Windows 11
- Windows 10/11 + WSL (Windows Subsystem for Linux)
- Ubuntu Linux

## Быстрый старт

> Все команды ниже выполняются из корня проекта (там, где лежат `README.md`, `pyproject.toml`, `uv.lock`).

### 1) Предварительные требования

- установленный **Git**
- установленный **uv** — менеджер Python-проектов (создаёт виртуальное окружение и ставит зависимости по `pyproject.toml`/`uv.lock`)

Проверить наличие uv:

```bash
uv --version
```

Если команда не находится, установите uv по официальной инструкции Astral.

### 2) Клонирование репозитория

```bash
git clone https://github.com/EPoY74/202312-todo.git
cd 202312-todo
```

### 3) Установка зависимостей (uv)

```bash
uv sync
```

Команда `uv sync` создаёт виртуальное окружение (обычно в `.venv`) и приводит зависимости к версиям из `uv.lock`.

## Запуск проекта

### 4) Инициализация базы данных (SQLite)

Перед первым использованием нужно создать файл SQLite-БД и таблицы:

```bash
uv run python -m src.cli --create_db
```

После успешного выполнения будет создан файл БД (по умолчанию — каталог `src/data/`).

#### Загрузка тестовых данных (опционально)

В каталоге `src/data/` есть скрипт `init_db.py`, который загружает дамп `sqlite-example-data-db-utf-8.sql` в базу `todo_main.db`.  
Если файл `todo_main.db` отсутствует — он будет создан автоматически.

```bash
uv run python src/data/init_db.py
```

### 5) Запуск CLI (консольное приложение)

Краткая справка по CLI:

```bash
uv run python -m src.cli
```

Типичные примеры:

```bash
# Показать список задач
uv run python -m src.cli --tasks_list
```

```bash
# Добавить новую задачу
uv run python -m src.cli --task_add "Купить молоко"
```

```bash
# Отметить задачу выполненной (по идентификатору)
uv run python -m src.cli --completed_at 1
```

```bash
# Удалить задачу (по идентификатору)
uv run python -m src.cli --task_del_id 1
```

### 6) Запуск REST API (опционально, экспериментально)

API-интерфейс позволяет получать данные о задачах по HTTP.

Запуск:

```bash
uv run python -m src.api
```

После старта в терминале будет указан адрес сервера (обычно `http://127.0.0.1:8000`).

Примеры эндпоинтов:

- `GET /` — информация о проекте / health check
- `GET /all` — список всех задач
- `GET /last` — последняя задача
- `GET /task/{id}` — задача по идентификатору

> Точный список и форматы запросов/ответов смотрите в OpenAPI/Swagger (если подключено) и в коде модуля `src.api`.

### 7) Где хранятся данные и логи

- **SQLite база данных**: `src/data/`
- **Логи CLI**: `src/log/` (файл `todo_cli.log` создаётся автоматически при первом запуске)

## License

Apache-2.0 — см. файл [LICENSE-ru](./LICENSE-ru)
